{
  "name": "kpsuperplane-personal-website",
  "version": "1.0.0",
  "description": "Kevin Pei's Personal Website",
  "author": "Kevin Pei",
  "repository": {
    "type": "git",
    "url": "git://github.com/kpsuperplane/personal-website.git"
  },
  "license": "MIT",
  "scripts": {
    "start": "node current/index.js",
    "preinstall": "export NODE_ENV=production && npm install -g yarn knex-migrator && cd current && yarn install --no-optional",
    "postinstall": "./config.sh && knex-migrator init --mgpath=current"
  },
  "engines": {
    "node": "^6.9.0"
  },
  "dependencies": {
  },
  "optionalDependencies": {
  },
  "devDependencies": {
  }
}