{
  "name": "kpsuperplane-personal-website",
  "version": "1.0.0",
  "description": "Kevin Pei's Personal Website",
  "author": "Kevin Pei",
  "repository": {
    "type": "git",
    "url": "git://github.com/kpsuperplane/personal-website.git"
  },
  "license": "MIT",
  "scripts": {
    "start": "node versions/1.5.1/index.js",
    "preinstall": "export NODE_ENV=production && npm install -g yarn && yarn global add knex-migrator && cd current && yarn install --no-optional",
    "postinstall": "./config.sh && NODE_ENV=production knex-migrator migrate --mgpath=versions/1.5.1/"
  },
  "engines": {
    "node": "^6.9.0"
  },
  "dependencies": {
  },
  "optionalDependencies": {
  },
  "devDependencies": {
  }
}